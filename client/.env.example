# Base URL for the API. Leave blank to use the Vite dev proxy (recommended in
# development), which forwards /api to the backend as a same-origin request so
# the refresh cookie is set first-party.
VITE_API_URL=

# Where the dev proxy forwards /api requests. Defaults to http://localhost:5000.
VITE_PROXY_TARGET=http://localhost:5000
